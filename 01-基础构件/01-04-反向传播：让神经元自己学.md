# 反向传播：让神经元自己学

## 历史背景：被遗忘的算法

### 1960-1970年代的困境

1969年，Minsky和Papert证明了单层感知机的局限性。研究者们意识到：**多层网络可能更强大**，但面临一个核心问题：

> **如何训练多层网络？单层感知机的学习规则无法直接推广到多层网络。**

当时的计算机学者们需要一个系统化的方法来计算"每个参数应该怎么调整"。

### 反向传播的早期发现

反向传播算法的思想其实很早就出现了：

- **1970年**：芬兰科学家 **Seppo Linnainmaa** 在他的硕士论文中首次描述了反向传播算法，但这篇论文没有引起广泛关注。
- **1974年**：美国科学家 **Paul Werbos** 在他的博士论文中独立发现了反向传播算法，并将其应用于神经网络，但同样没有得到重视。

### 1986年的复兴

1986年，**David Rumelhart**、**Geoffrey Hinton** 和 **Ronald Williams** 发表了里程碑式的论文：

> *"Learning representations by back-propagating errors"*

他们重新发现并推广了反向传播算法，证明了它在训练多层网络中的强大能力。这篇论文直接引发了神经网络研究的**第二次热潮**。

### 为什么叫"反向传播"？

因为梯度是从**输出层向输入层反向传播**的：

```
前向传播：输入 → 隐藏层 → 输出 → 损失
反向传播：损失 → 输出层 → 隐藏层 → 输入层（梯度）
```

---

## 1. 为什么需要反向传播

上一章我们学习了训练的基础：损失函数和梯度下降。但还剩一个关键问题——**如何高效计算多层网络中所有参数的梯度**？

对于单层网络，计算梯度很简单。但对于多层网络，参数有成千上万个，如何高效计算所有梯度？

**反向传播（Backpropagation）** 就是这个方法。

---

## 2. 核心思想：链式法则

### 2.1 一个简单例子

回顾上一章梯度下降我们讲过的例子：

对于单个神经元，假设我们有：

$$
L = (y - \hat{y})^2, \quad \hat{y} = wx + b
$$

其中：

- **$L$** 是损失函数（Loss），衡量预测值与真实值的差距
- **$y$** 是真实标签/真实值（ground truth）
- **$\hat{y}$** 是预测值（prediction），即模型的输出

我们需要调整的是权重参数 $w$ 。参数 $w$**往哪个方向调、调整多少**，能让损失 $L$ 下降最多。我们可以通过求损失 $L$ 对参数 $w$ 的偏导数：

- 若 $\frac{\partial L}{\partial w} > 0$，说明增大 $w$ 会增大 $L$，所以要减小 $w$
- 若 $\frac{\partial L}{\partial w} < 0$，说明增大 $w$ 会减小 $L$，所以要增大 $w$

更新规则：$w \leftarrow w - \eta \cdot \frac{\partial L}{\partial w}$（梯度下降）

更新规则中，只有$\frac{\partial L}{\partial w}$不是确定的（权重有初始化值、步长也是固定值）。

而想求 $\frac{\partial L}{\partial w}$，通过微积分知识，我们知道可以这样转换：

$$
\frac{\partial L}{\partial w} = \frac{\partial L}{\partial \hat{y}} \cdot \frac{\partial \hat{y}}{\partial w}
$$

这就是**链式法则**——把复杂梯度拆成简单步骤。

#### 2.3 为什么链式法则有效（直觉解释）

为什么链式法则能把复杂梯度拆成简单步骤？让我们用一个生活中的例子来理解。

**复合函数就像"套娃"**

在数学中，"复合函数"就是**函数套函数**。比如上面的例子：

$$
L = (\hat{y} - y)^2, \quad \hat{y} = wx + b
$$

损失 $L$ 里面套着预测值 $\hat{y}$，而 $\hat{y}$ 里面又套着参数 $w$。这就像俄罗斯套娃：打开最外层，里面还有一层，再打开还有一层。

当我们想知道"调整 $w$ 会对 $L$ 产生多大影响"时，需要一层一层地追踪这个影响：

$$
w \xrightarrow{\text{影响}} \hat{y} \xrightarrow{\text{影响}} L
$$

**生活中的类比：快递运费**

想象你要计算一个包裹从发货地到收货地的**总运费**。运费由三段组成：

```
发货地 → 中转站A → 中转站B → 收货地
```

每一段都有自己的费用规则：

- 第一段（发货地→中转站A）：距离 × 单价 3 元/公里
- 第二段（中转站A→中转站B）：重量 × 单价 2 元/公斤
- 第三段（中转站B→收货地）：固定费用 10 元

假设距离是 50 公里，重量是 5 公斤。直接计算总运费：

$$
\text{总运费} = 50 \times 3 + 5 \times 2 + 10 = 170 \text{ 元}
$$

现在问题是：**如果距离增加 1 公里，总运费增加多少？**

**方法一：直接计算**

让距离从 50 变成 51，重新算：

$$
\text{新运费} = 51 \times 3 + 5 \times 2 + 10 = 173 \text{ 元}
$$

变化量 = 173 - 170 = **3 元**

**方法二：链式法则思维**

逐段分析"距离对总运费的影响"：

1. 距离对第一段费用的影响：**每增加 1 公里 → 第一段费用增加 3 元**（单价决定的）
2. 第一段费用对总运费的影响：**第一段费用增加多少 → 总运费就增加多少**（因为是直接相加）

把两步"链接"起来：

$$
\frac{\partial \text{总运费}}{\partial \text{距离}} = \frac{\partial \text{总运费}}{\partial \text{第一段费用}} \times \frac{\partial \text{第一段费用}}{\partial \text{距离}} = 1 \times 3 = 3
$$

两种方法得到相同结果！链式法则的魔力在于：**把一个复杂的变化关系，拆成几个简单的"局部"关系，然后乘起来**。

**数值验证：回到神经网络的例子**

设 $x = 2$, $w = 3$, $b = 1$, $y = 0.5$。

直接计算梯度：

$$
\hat{y} = wx + b = 3 \times 2 + 1 = 7
$$

$$
L = (y - \hat{y})^2 = (0.5 - 7)^2 = 42.25
$$

用链式法则分步求导：

$$
\frac{\partial L}{\partial \hat{y}} = 2(\hat{y} - y) = 2 \times (7 - 0.5) = 13
$$

$$
\frac{\partial \hat{y}}{\partial w} = x = 2
$$

$$
\frac{\partial L}{\partial w} = \frac{\partial L}{\partial \hat{y}} \times \frac{\partial \hat{y}}{\partial w} = 13 \times 2 = 26
$$

直接求导验证：

$$
\frac{\partial L}{\partial w} = \frac{\partial}{\partial w}(y - wx - b)^2 = 2(y - wx - b) \times (-x) = 2 \times (-6.5) \times (-2) = 26
$$

**结果一致！**

**链式法则的核心洞见**

链式法则之所以有效，是因为"变化"是可以传递的：

$$
\text{w 的变化} \xrightarrow{\times \frac{\partial \hat{y}}{\partial w}} \hat{y} \text{ 的变化} \xrightarrow{\times \frac{\partial L}{\partial \hat{y}}} L \text{ 的变化}
$$

每一层只需要关心**局部的导数**——这一层的输出对输入的变化率。然后把这些局部导数乘起来，就得到了整体的导数。

**反向传播就是链式法则的工程化实现**

当我们把链式法则应用到多层神经网络时，就得到了反向传播算法：

- 每一层只需要计算"自己这一层"的局部梯度
- 把梯度从后往前一层层传递、相乘
- 最终得到每个参数对损失的影响

这就是为什么反向传播能高效计算成千上万个参数的梯度——它不需要对整个网络求导，只需要每层做简单的乘法。

### 2.4 计算图

把计算过程画成图：

```
x ──→ [×w] ──→ [+] ──→ ŷ ──→ [L] ──→ L
       ↑        ↑
       w        b
```

**前向传播**：从左到右计算值
**反向传播**：从右到左计算梯度

---

## 3. 反向传播的步骤

### 3.1 完整流程

```
1. 前向传播：从输入计算到输出，得到预测值
2. 计算损失：比较预测值和真实值
3. 反向传播：从损失出发，逐层计算梯度
4. 更新参数：w ← w - η · ∂L/∂w
```

### 3.2 数学推导

对于两层网络：

**前向传播：**

$$
z_1 = W_1 x + b_1 \quad \text{(隐藏层线性变换)}
$$

$$
a_1 = \sigma(z_1) \quad \text{(隐藏层激活)}
$$

$$
z_2 = W_2 a_1 + b_2 \quad \text{(输出层线性变换)}
$$

$$
\hat{y} = \sigma(z_2) \quad \text{(输出层激活)}
$$

$$
L = (\hat{y} - y)^2 \quad \text{(损失)}
$$

**反向传播：**

输出层梯度：

$$
\frac{\partial L}{\partial \hat{y}} = 2(\hat{y} - y)
$$

$$
\frac{\partial L}{\partial z_2} = \frac{\partial L}{\partial \hat{y}} \cdot \sigma'(z_2)
$$

$$
\frac{\partial L}{\partial W_2} = \frac{\partial L}{\partial z_2} \cdot a_1^T
$$

$$
\frac{\partial L}{\partial b_2} = \frac{\partial L}{\partial z_2}
$$

隐藏层梯度（链式法则继续传播）：

$$
\frac{\partial L}{\partial a_1} = W_2^T \cdot \frac{\partial L}{\partial z_2}
$$

$$
\frac{\partial L}{\partial z_1} = \frac{\partial L}{\partial a_1} \cdot \sigma'(z_1)
$$

$$
\frac{\partial L}{\partial W_1} = \frac{\partial L}{\partial z_1} \cdot x^T
$$

$$
\frac{\partial L}{\partial b_1} = \frac{\partial L}{\partial z_1}
$$

更新参数：

$$
W_1 \leftarrow W_1 - \eta \cdot \frac{\partial L}{\partial W_1}, \quad b_1 \leftarrow b_1 - \eta \cdot \frac{\partial L}{\partial b_1}
$$

$$
W_2 \leftarrow W_2 - \eta \cdot \frac{\partial L}{\partial W_2}, \quad b_2 \leftarrow b_2 - \eta \cdot \frac{\partial L}{\partial b_2}
$$

---

## 4. 激活函数：让网络"活"起来

### 4.1 什么是激活函数？

在上面的两层网络推导中，我们看到了激活函数的关键作用：

隐藏层：

$$
z_1 = W_1 x + b_1
$$

$$
a_1 = \sigma(z_1)
$$

输出层：

$$
z_2 = W_2 a_1 + b_2
$$

$$
\hat{y} = \sigma(z_2)
$$

**激活函数**就是这个 $\sigma$，它作用在每一层线性变换的结果上，产生该层的输出。

**为什么叫"激活"？** 这个名字来自生物神经元：当输入信号足够强时，神经元会"激活"（发放脉冲）。激活函数模拟了这个过程——决定神经元是否应该"激活"。

### 4.2 为什么需要激活函数？

**如果没有激活函数**，无论多少层网络：

$$
y = W_3(W_2(W_1 x + b_1) + b_2) + b_3 = W_{combined} \cdot x + b_{combined}
$$

**结果仍然是线性的！** 本质上和单层网络一样。

激活函数引入**非线性**，让网络能够学习复杂模式。这就是为什么：

- 单层感知机只能学线性问题（AND、OR）
- 多层网络 + 激活函数可以学非线性问题（XOR）

### 4.3 常见的激活函数

在反向传播中，我们需要激活函数的**导数**。不同的激活函数有不同的导数：

**Sigmoid**（历史最悠久）：

$$
\sigma(z) = \frac{1}{1 + e^{-z}}, \quad \sigma'(z) = \sigma(z)(1 - \sigma(z))
$$

- 输出范围：(0, 1)
- 导数最大值：0.25（导致梯度消失）

**ReLU**（现代最常用）：

$$
\text{ReLU}(z) = \max(0, z), \quad \text{ReLU}'(z) = \begin{cases} 1 & z > 0 \\ 0 & z \leq 0 \end{cases}
$$

- 计算简单，只需判断正负
- 正区间梯度恒为1，缓解梯度消失

**Tanh**（Sigmoid的改进）：

$$
\tanh(z) = \frac{e^z - e^{-z}}{e^z + e^{-z}}, \quad \tanh'(z) = 1 - \tanh^2(z)
$$

- 输出范围：(-1, 1)，零中心
- 比Sigmoid好，但仍有梯度消失问题

### 4.4 激活函数在反向传播中的作用

在反向传播中，激活函数的导数是关键：

$$
\frac{\partial L}{\partial z} = \frac{\partial L}{\partial a} \cdot \sigma'(z)
$$

**这意味着**：

- 如果 $\sigma'(z)$ 很小（比如Sigmoid在饱和区），梯度会很小，参数更新慢
- 如果 $\sigma'(z) = 0$（比如ReLU在负区间），梯度完全消失，参数不再更新
- 如果 $\sigma'(z) = 1$（比如ReLU在正区间），梯度直接传递，训练高效

**这就是为什么选择激活函数如此重要**——它直接影响梯度能否有效传播。

### 4.5 为下一章做铺垫

在下一章"激活函数与权重初始化"中，我们将深入探讨：

- 各种激活函数的详细对比
- 如何根据网络结构选择合适的激活函数
- 权重初始化如何与激活函数配合

**核心思想**：激活函数和权重初始化是"一对搭档"——激活函数决定非线性能力，权重初始化决定信号能否稳定传播。

---

## 5. 计算图与自动微分

### 5.1 什么是计算图

**计算图（Computation Graph）** 是一种有向无环图（DAG），它把数学运算表示为图结构：

- **节点（Node）**：表示变量（输入、输出、中间结果）
- **边（Edge）**：表示运算操作

#### 计算图示例

以函数 $f(x, y) = (x + y) \times y$ 为例：

```
    x ──┐
        ├──→ [+] ──→ c ──┐
    y ──┘                ├──→ [×] ──→ f
                         │
    y ──────────────────┘
```

**分解步骤**：
- $c = x + y$（加法运算，中间变量）
- $f = c \times y$（乘法运算，最终输出）

#### 前向传播：在计算图上正向计算

设 $x = 2, y = 3$，前向传播过程：

| 步骤 | 计算 | 结果 |
|------|------|------|
| 1 | $c = x + y = 2 + 3$ | $c = 5$ |
| 2 | $f = c \times y = 5 \times 3$ | $f = 15$ |

### 5.2 自动微分：为什么反向传播能自动求导

**自动微分（Automatic Differentiation, AD）** 是计算机计算梯度的核心技术。它不同于：

- **数值微分**：用有限差分近似（如 $\frac{f(x+h)-f(x)}{h}$），有截断误差
- **符号微分**：代数推导公式（如手动求导），表达式可能指数膨胀

**自动微分的核心思想**：
> 所有复杂的数学运算，都可以分解成一系列**基本运算**（加、减、乘、除、指数、对数等）。每个基本运算的导数都是已知的，通过链式法则组合就能得到复杂函数的梯度。

#### 自动微分的两种模式

**1. 前向模式（Forward Mode）**

同时传播**值**和**梯度**。对于每个中间变量 $v$，我们追踪一对值 $(v, \frac{\partial v}{\partial x})$。

**示例**：求 $f(x, y) = (x + y) \times y$ 在 $x=2, y=3$ 处对 $x$ 的偏导。

初始化：
- $x = (2, 1)$（值为2，对x的导数为1）
- $y = (3, 0)$（值为3，对x的导数为0）

前向传播同时计算值和导数：

| 运算 | 值计算 | 导数计算 | 结果 |
|------|--------|----------|------|
| $c = x + y$ | $2 + 3 = 5$ | $1 + 0 = 1$ | $(5, 1)$ |
| $f = c \times y$ | $5 \times 3 = 15$ | $1 \times 3 + 5 \times 0 = 3$ | $(15, 3)$ |

得到 $\frac{\partial f}{\partial x} = 3$。

**手动验证**：
$$f = (x + y)y = xy + y^2$$
$$\frac{\partial f}{\partial x} = y = 3 \checkmark$$

**2. 反向模式（Reverse Mode）**

**这是反向传播的核心！**

反向模式先进行**前向传播**计算所有中间值，然后从输出开始**反向传播梯度**。

**关键洞察**：
> 对于函数 $f(x_1, x_2, ..., x_n)$，如果输出是标量（如损失函数），反向模式只需**一次遍历**就能计算出对所有输入的梯度，效率远高于前向模式（需要n次遍历）。这正是神经网络需要的！

#### 反向模式详细推导

以求 $\frac{\partial f}{\partial x}$ 和 $\frac{\partial f}{\partial y}$ 为例：

**第一步：前向传播（记录所有中间值）**

```
    x=2 ──┐
          ├──→ [+] ──→ c=5 ──┐
    y=3 ──┘                  ├──→ [×] ──→ f=15
                             │
    y=3 ────────────────────┘
```

**第二步：反向传播（从输出向输入传播梯度）**

我们从 $\bar{f} = \frac{\partial f}{\partial f} = 1$ 开始，逆向应用链式法则。

**链式法则回顾**：
如果 $z = f(y)$ 且 $y = g(x)$，则：
$$\frac{\partial z}{\partial x} = \frac{\partial z}{\partial y} \cdot \frac{\partial y}{\partial x}$$

在反向模式中，我们用 $\bar{v} = \frac{\partial f}{\partial v}$ 表示**最终输出对中间变量v的梯度**。

**反向传播过程**：

| 节点 | 局部梯度规则 | 反向梯度计算 | 结果 |
|------|-------------|--------------|------|
| $f = c \times y$ | $\frac{\partial f}{\partial c} = y$, $\frac{\partial f}{\partial y} = c$ | $\bar{c} = \bar{f} \cdot y = 1 \times 3 = 3$<br>$\bar{y}^{(1)} = \bar{f} \cdot c = 1 \times 5 = 5$ | $\bar{c}=3$, $\bar{y}^{(1)}=5$ |
| $c = x + y$ | $\frac{\partial c}{\partial x} = 1$, $\frac{\partial c}{\partial y} = 1$ | $\bar{x} = \bar{c} \cdot 1 = 3$<br>$\bar{y}^{(2)} = \bar{c} \cdot 1 = 3$ | $\bar{x}=3$, $\bar{y}^{(2)}=3$ |
| **合并y的梯度** |  | $\bar{y} = \bar{y}^{(1)} + \bar{y}^{(2)} = 5 + 3 = 8$ | $\bar{y}=8$ |

**梯度合并解释**：
$y$ 在计算图中被使用了两次（一次在$c=x+y$，一次在$f=c \times y$）。根据多元链式法则，当变量有多个下游使用时，梯度要**累加**：

$$\frac{\partial f}{\partial y} = \frac{\partial f}{\partial c}\frac{\partial c}{\partial y} + \frac{\partial f}{\partial y}\bigg|_{\text{乘法}}$$

**手动验证**：
$$f = xy + y^2$$
$$\frac{\partial f}{\partial x} = y = 3 \checkmark$$
$$\frac{\partial f}{\partial y} = x + 2y = 2 + 6 = 8 \checkmark$$

### 5.3 计算图在神经网络中的应用

现在我们把反向模式应用到神经元：

$$L = (y - \hat{y})^2, \quad \hat{y} = \sigma(wx + b)$$

#### 构建计算图

```
x ──→ [×] ──→ [+] ──→ [σ] ──→ ŷ ──→ [-] ──→ [²] ──→ L
      ↑       ↑
      w       b
                    
y ────────────────────────→
```

#### 前向传播（计算值）

设 $x=2, w=3, b=1, y=0.5$：

| 节点 | 运算 | 结果 |
|------|------|------|
| $z$ | $w \times x = 3 \times 2$ | $z = 6$ |
| $a$ | $z + b = 6 + 1$ | $a = 7$ |
| $\hat{y}$ | $\sigma(7) = \frac{1}{1+e^{-7}}$ | $\hat{y} \approx 0.999$ |
| $d$ | $\hat{y} - y = 0.999 - 0.5$ | $d \approx 0.499$ |
| $L$ | $d^2 = 0.499^2$ | $L \approx 0.249$ |

#### 反向传播（计算梯度）

从 $\bar{L} = 1$ 开始：

| 节点 | 运算规则 | 局部梯度 | 反向梯度计算 |
|------|----------|----------|--------------|
| $L = d^2$ | $\frac{\partial L}{\partial d} = 2d$ | $2 \times 0.499 \approx 0.998$ | $\bar{d} = 1 \times 0.998 = 0.998$ |
| $d = \hat{y} - y$ | $\frac{\partial d}{\partial \hat{y}} = 1$ | 1 | $\bar{\hat{y}} = 0.998 \times 1 = 0.998$ |
| $\hat{y} = \sigma(a)$ | $\frac{\partial \hat{y}}{\partial a} = \sigma(a)(1-\sigma(a))$ | $0.999 \times 0.001 \approx 0.001$ | $\bar{a} = 0.998 \times 0.001 \approx 0.001$ |
| $a = z + b$ | $\frac{\partial a}{\partial z} = 1$<br>$\frac{\partial a}{\partial b} = 1$ | 1, 1 | $\bar{z} = 0.001 \times 1 = 0.001$<br>$\bar{b} = 0.001 \times 1 = 0.001$ |
| $z = w \times x$ | $\frac{\partial z}{\partial w} = x$<br>$\frac{\partial z}{\partial x} = w$ | 2, 3 | $\bar{w} = 0.001 \times 2 = 0.002$<br>$\bar{x} = 0.001 \times 3 = 0.003$ |

**最终结果**：
$$\frac{\partial L}{\partial w} \approx 0.002, \quad \frac{\partial L}{\partial b} \approx 0.001$$

### 5.4 为什么反向传播就是反向模式自动微分

**反向传播（Backpropagation）= 反向模式自动微分（Reverse-Mode AD）**

在神经网络中：
- **损失函数** $L$ 是标量（单个数值）
- **参数**数量巨大（可能数百万个）
- **输入**是训练数据（不需要对输入求导）

**反向模式的优势**：
1. **一次反向遍历**，计算所有参数的梯度
2. 时间复杂度与网络规模成线性关系 $O(n)$
3. 空间复杂度需要存储前向传播的所有中间值 $O(n)$

**对比前向模式**：
- 如果对每个参数都用前向模式，需要 $O(n^2)$ 时间
- 对于神经网络来说，前向模式完全不可行

### 5.5 动态计算图（PyTorch实现）

现代框架（如PyTorch）使用**动态计算图**：

```python
import torch

x = torch.tensor([2.0], requires_grad=True)
w = torch.tensor([3.0], requires_grad=True)
b = torch.tensor([1.0], requires_grad=True)

# 前向传播（自动构建计算图）
z = w * x + b      # 创建计算图节点
a = torch.sigmoid(z)
loss = (a - 0.5) ** 2

# 反向传播（自动计算梯度）
loss.backward()

print(f"∂L/∂w = {w.grad}")  # 链式法则自动计算
print(f"∂L/∂b = {b.grad}")
```

**PyTorch在背后做了什么？**

1. **前向传播时**：每个运算都创建一个`Function`对象，记录输入和运算类型，形成图结构
2. **反向传播时**：调用`.backward()`，框架从输出节点开始，沿着图的反方向，自动应用链式法则

### 5.6 自动微分的魔力总结

| 特性 | 说明 |
|------|------|
| **精确性** | 没有数值误差，结果是精确的（只受浮点精度限制） |
| **通用性** | 任何可微程序都可以自动求导，包括条件、循环等控制流 |
| **高效性** | 反向模式对标量输出最优，时间复杂度 $O(n)$ |
| **易用性** | 用户只需定义前向计算，梯度自动计算 |

**核心洞见**：
> 自动微分把复杂的求导问题，分解成一系列**局部的、简单的**求导规则。反向传播就是从损失函数出发，沿着计算图的反方向，逐节点应用这些局部规则，通过链式法则累积得到最终的参数梯度。

这就是为什么我们说"反向传播让神经元自己学习"——你只需定义网络结构和损失函数，框架自动帮你计算"每个参数应该如何调整"。

**关键公式总结**：

对于计算图中的任意节点 $v$，如果它有多个子节点 $u_1, u_2, ...$，则：

$$\bar{v} = \sum_{i} \bar{u_i} \cdot \frac{\partial u_i}{\partial v}$$

这正是反向传播算法的数学本质。

---

## 6. MLP 的反向传播

### 5.1 回顾网络结构

以上一章的两层 MLP 为例：

```
输入层    隐藏层    输出层
 x₁  ───→  h₁  ───→  y₁
    ╲    ╱ ╲    ╱
     ╲  ╱   ╲  ╱
     ╱  ╲   ╱  ╲
    ╱    ╲ ╱    ╲
 x₂  ───→  h₂  ───→  y₂
```

### 5.2 逐层反向传播

```python
# 前向传播
h = W1 @ x + b1          # 隐藏层线性变换
h_act = relu(h)          # 隐藏层激活
y = W2 @ h_act + b2      # 输出层线性变换

# 反向传播（从后往前）
dy = y - y_true                  # 输出层梯度
dW2 = dy @ h_act.T               # W2 的梯度
db2 = dy                         # b2 的梯度

dh_act = W2.T @ dy               # 传到隐藏层
dh = dh_act * (h > 0)            # ReLU 的导数

dW1 = dh @ x.T                   # W1 的梯度
db1 = dh                         # b1 的梯度

# 更新参数
W1 -= lr * dW1
b1 -= lr * db1
W2 -= lr * dW2
b2 -= lr * db2
```

---

## 7. 完整代码示例

本节代码示例演示用两层神经网络解决 **XOR（异或）问题**：

- 输入：(0,0)、(0,1)、(1,0)、(1,1)
- 期望输出：0、1、1、0（异或逻辑）

XOR 是经典非线性问题，单层感知机无法解决，但两层网络+激活函数可以学习。

```python
import numpy as np

def sigmoid(x):
    return 1 / (1 + np.exp(-np.clip(x, -500, 500)))

def sigmoid_derivative(x):
    s = sigmoid(x)
    return s * (1 - s)

> 📚 **前置知识提示**
> 
> 下面的代码使用了 **Xavier/He 初始化** 来设置初始权重：
> - 如果不特殊设置，随机权重可能太大或太小，导致信号在深层网络中**消失**或**爆炸**
> - `sqrt(2/n)` 根据输入神经元数量调整权重范围，保持信号稳定传播
> - 详细原理（为什么用2、为什么除以n）将在[第5章：激活函数与权重初始化](01-05-激活函数与权重初始化.md)中讲解
> 
> 现在只需知道：这是一种让深层网络能够训练的重要技巧。

class TwoLayerNet:
    def __init__(self, n_input, n_hidden, n_output):
        # Xavier 初始化
        self.W1 = np.random.randn(n_hidden, n_input) * np.sqrt(2.0 / n_input)
        self.b1 = np.zeros((n_hidden, 1))
        self.W2 = np.random.randn(n_output, n_hidden) * np.sqrt(2.0 / n_hidden)
        self.b2 = np.zeros((n_output, 1))
  
    def forward(self, X):
        """前向传播"""
        self.z1 = self.W1 @ X + self.b1  # @ = np.dot() 矩阵乘法。它是一个通过运算符重载实现的语法糖。
        self.a1 = sigmoid(self.z1)
        self.z2 = self.W2 @ self.a1 + self.b2
        self.a2 = sigmoid(self.z2)
        return self.a2
  
    def backward(self, X, Y):
        """反向传播"""
        m = X.shape[1]
  
        # 输出层梯度
        dz2 = self.a2 - Y  # 假设用 MSE 损失
        dW2 = (1/m) * dz2 @ self.a1.T
        db2 = (1/m) * np.sum(dz2, axis=1, keepdims=True)
  
        # 隐藏层梯度
        da1 = self.W2.T @ dz2
        dz1 = da1 * sigmoid_derivative(self.z1)
        dW1 = (1/m) * dz1 @ X.T
        db1 = (1/m) * np.sum(dz1, axis=1, keepdims=True)
  
        return dW1, db1, dW2, db2
  
    def train(self, X, Y, epochs=1000, lr=0.1):
        for epoch in range(epochs):
            # 前向传播
            output = self.forward(X)
  
            # 计算损失
            loss = np.mean((output - Y) ** 2)
  
            # 反向传播
            dW1, db1, dW2, db2 = self.backward(X, Y)
  
            # 更新参数
            self.W1 -= lr * dW1
            self.b1 -= lr * db1
            self.W2 -= lr * dW2
            self.b2 -= lr * db2
  
            if epoch % 100 == 0:
                print(f"Epoch {epoch}, Loss: {loss:.4f}")

# 测试 XOR 问题（单层感知机学不了，两层可以）
X = np.array([[0, 0, 1, 1], [0, 1, 0, 1]])
Y = np.array([[0, 1, 1, 0]])

net = TwoLayerNet(2, 4, 1)
net.train(X, Y, epochs=5000, lr=1.0)

print("\n预测结果：")
print(net.forward(X))
```

---

## 8. 常见问题

**Q: 梯度消失是什么？**

当网络很深时，梯度在反向传播中逐层相乘。如果每层的梯度都小于 1，最终梯度会趋近于 0：

$$
\frac{\partial L}{\partial W_1} = \frac{\partial L}{\partial a_n} \cdot \sigma'_n \cdot W_n \cdot \ldots \cdot \sigma'_1
$$

如果每层 $\sigma' \cdot W < 1$，乘积会指数衰减。

**Q: 梯度爆炸是什么？**

反过来，如果每层梯度都大于 1，梯度会指数增长，导致参数更新过大、训练不稳定。

**解决方案**：

- 使用 ReLU 替代 Sigmoid（ReLU 导数要么是 0 要么是 1）
- 梯度裁剪（Gradient Clipping）
- 残差连接（ResNet 的核心思想）

---

## 9. 小结

### 9.1 反向传播的四个步骤

| 步骤     | 做什么                                                        |
| -------- | ------------------------------------------------------------- |
| 前向传播 | 从输入到输出，计算预测值                                      |
| 计算损失 | 衡量预测与真实的差距                                          |
| 反向传播 | 用链式法则计算每个参数的梯度                                  |
| 参数更新 | $w \leftarrow w - \eta \cdot \frac{\partial L}{\partial w}$ |

### 9.2 与上一章的概念衔接

回顾我们在[第3章：训练的基础](01-03-训练的基础：损失函数与梯度下降.md)中讨论的问题：

> **梯度下降需要梯度，但梯度怎么算？**

**本章给出了答案**：

```
第3章（训练的基础）          第4章（反向传播）
─────────────────────      ─────────────────────
损失函数 L(θ)    ─────┐
                     │ 需要
梯度下降 θ←θ-η·∇L ◄──┤
                     │ 提供
梯度 ∇L  ────────────┘
                          ↑
                     计算图 + 自动微分
                     （反向模式）
```

**完整训练流程的概念关系**：

| 层次 | 概念 | 作用 | 实现方式 |
|------|------|------|----------|
| **目标** | 损失函数 | 衡量预测好坏 | 手工设计（MSE、交叉熵等） |
| **优化** | 梯度下降 | 最小化损失 | 参数更新规则 |
| **计算** | 自动微分 | 高效计算梯度 | 反向传播算法 |
| **基础** | 链式法则 | 复合函数求导 | 数学原理 |

**核心洞见**：

1. **梯度下降是优化策略**——决定"如何用梯度更新参数"
2. **自动微分是计算技术**——解决"如何高效获得梯度"
3. **反向传播是自动微分在神经网络中的具体实现**——利用网络结构和链式法则，高效计算所有参数的梯度

正是因为有了自动微分和反向传播，我们才能在拥有数百万参数的深层神经网络中应用梯度下降，让"神经元自己学习"成为可能。

---

## 延伸阅读

- CS231n 反向传播笔记
- Rumelhart et al. (1986) "Learning representations by back-propagating errors" - 反向传播算法的里程碑论文
- Werbos (1974) "Beyond Regression: New Tools for Prediction and Analysis in the Behavioral Sciences" - Paul Werbos的博士论文，独立发现了反向传播算法
- Linnainmaa (1970) "The representation of the cumulative rounding error of an algorithm as a Taylor expansion of the local rounding errors" - 首次描述反向传播算法的硕士论文
